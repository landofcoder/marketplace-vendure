{
  "name": "@bavaan/vendure-product-recently-viewed-plugin",
  "version": "0.0.1",
  "description": "A product recently-viewed plugin for Bavaan Marketplace Platform",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:coverage-ci": "jest --coverage && coveralls < coverage/lcov.info",
    "build": "rimraf dist && tsc -p ./tsconfig.json",
    "generate-types": "graphql-codegen --config ./codegen.json"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/ms": "^0.7.31",
    "@types/node": "^14.0.13",
    "coveralls": "^3.1.0",
    "jest": "^26.0.1",
    "ts-jest": "^26.1.0",
    "typescript": "4.0.3"
  },
  "dependencies": {
  }
}